Image file nudity detection script.


USAGE:
    $ python3 -m venv nudeenv
    $ nudeenv\Scripts\activate
    $ pip install -r .\requirements.txt

    - create "images" directory and put your images inside it.

    $ python .\is_nude.py